// main function is the first code that runs in every executable rust program
fn main() {
    // ! at the end of println signifies that it is a macro and not a normal function
    // end line with semicolon
    println!("Hello, world!");
}
// to compile, run "rustc main.rs"
// to run executable, run "./main"